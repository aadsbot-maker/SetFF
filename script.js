const searchInput=document.getElementById('search');const phoneList=document.getElementById('phoneList');const settingsDiv=document.getElementById('settings');const deviceName=document.getElementById('deviceName');fetch('phones.json').then(res=>res.json()).then(data=>{Object.keys(data).forEach(name=>{const li=document.createElement('li');li.textContent=name;li.setAttribute('data-name',name);li.addEventListener('click',()=>{const settings=data[name];settingsDiv.innerHTML=`<h2>${name}</h2><div class="setting-item"><strong>DPI:</strong> ${settings.dpi}</div><div class="setting-item"><strong>General:</strong> ${settings.general}</div><div class="setting-item"><strong>Red Dot:</strong> ${settings.redDot}</div><div class="setting-item"><strong>2x Scope:</strong> ${settings.x2}</div><div class="setting-item"><strong>4x Scope:</strong> ${settings.x4}</div><div class="setting-item"><strong>AWM Scope:</strong> ${settings.awm}</div><div class="setting-item"><strong>Free Look:</strong> ${settings.freeLook}</div>`;settingsDiv.style.display='block';});phoneList.appendChild(li);});searchInput.addEventListener('keyup',()=>{const filter=searchInput.value.toLowerCase();Array.from(phoneList.getElementsByTagName('li')).forEach(item=>{const text=item.textContent.toLowerCase();item.style.display=text.includes(filter)?'':'none';});});});